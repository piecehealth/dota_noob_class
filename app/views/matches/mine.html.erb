<div class="py-6 px-3 sm:px-6">
  <div class="max-w-4xl mx-auto bg-white rounded-2xl shadow-xl overflow-hidden">
    <div class="p-4 sm:p-8">
      <h1 class="text-2xl font-bold mb-6">我的对战记录</h1>

      <%= render "matches/match_list", matches: @matches, weekly_limit_reached: @weekly_limit_reached %>

      <%= paginate @matches %>
    </div>
  </div>
</div>
